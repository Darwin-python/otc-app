# 🧩 OTC App — Telegram OTC Parser & Reputation Bot

**OTC App** — это Docker-основанное приложение, которое включает:

- 📥 **Telegram User Parser** — сбор сообщений и активности через Telethon  
- 🤖 **Telegram Bot** (Aiogram) — обработка реакций, репутации и статистики  
- 🗄 **PostgreSQL-совместимый модуль хранения**  
- ⚙️ **Аналитический модуль для классификации и структуры тем**

Проект создан для автоматизации работы OTC‑сообщества (WTB/WTS), анализа активности и удобного взаимодействия пользователей.

---

## 🚀 Возможности

### 🟣 Telegram User Collector (userbot)

- Подключается как userbot через Telethon  
- Собирает сообщения, пользователей, реакцию, тему  
- Сохраняет всё в базу (через `db.py`)  
- Работает в Docker

### 🔵 Telegram Bot (Aiogram)

- Добавляет и убирает реакции  
- Считает репутацию пользователей  
- Выводит статистику (количество сообщений, реакций, активность)  
- Работает в реальном времени

### 🟠 Аналитический модуль

- Индексация чатов через `otc_chats_index.json`  
- Топики/категории сообщений  
- Подготовка структурированных данных

---

## 📦 Установка и запуск (Docker)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/Darwin-python/otc-app
cd otc-app
```

### 2. Создать файл `.env` в папке `update`

```env
API_ID=your_api_id
API_HASH=your_api_hash
BOT_TOKEN=your_bot_token
TARGET_GROUP=your_target_group
```

### 3. Создать директории `data` и `sessions`

```bash
mkdir -p data sessions
```

### 4. Запустить контейнер

```bash
docker-compose up -d --build
```

### 5. Просматривать логи

```bash
docker logs -f otc-app
```

---

## 🧱 Структура проекта

```text
otc-app/
│
├── analysis/
│   ├── create_topics.py
│   └── test.py
│
├── deal_items_extractor/
│   ├── deal_items_extractor.py
│   ├── deal_items_new_candidates.txt
│   ├── deal_items_per_message.csv
│   └── deal_items_top.csv
│
├── update/
│   ├── bot.py
│   ├── db.py
│   ├── t_collector.py
│   ├── tools/
│   └── topics.json
│
├── data/
├── sessions/
│
├── otc_chats_index.json
├── docker-compose.yml
├── Dockerfile
├── entrypoint.sh
└── requirements.txt
```

---

## ⚙️ Переменные окружения

| Переменная      | Описание                                   |
|-----------------|---------------------------------------------|
| `API_ID`        | Telegram API ID                             |
| `API_HASH`      | Telegram API HASH                           |
| `BOT_TOKEN`     | Токен Telegram‑бота                         |
| `TARGET_GROUP`  | Имя или ID целевой группы                   |

---

## 🧪 Локальный запуск (без Docker)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск парсера

```bash
python update/t_collector.py
```

### Запуск бота

```bash
python update/bot.py
```

---

## 🧿 Как работает система

```text
[Telethon Userbot] → t_collector.py → db.py → база данных / файлы
                                         ↓
[Aiogram Bot] → bot.py → реакции, репутация, статистика
```

---

## 📜 Лицензия

MIT License

---

## 👤 Автор

**Darwin Studios**  
Telegram: @studios_by_darwin  
GitHub: https://github.com/Darwin-python

---

## ⭐ Поддержите проект звездой!
